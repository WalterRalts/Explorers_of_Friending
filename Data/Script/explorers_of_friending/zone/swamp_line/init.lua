--[[
    init.lua
    Created: 07/16/2025 19:49:22
    Description: Autogenerated script file for the map swamp_line.
]]--
-- Commonly included lua functions and data
require 'origin.common'

-- Package name
local swamp_line = {}

-------------------------------
-- Zone Callbacks
-------------------------------
---swamp_line.Init(zone)
--Engine callback function
function swamp_line.Init(zone)


end

---swamp_line.EnterSegment(zone, rescuing, segmentID, mapID)
--Engine callback function
function swamp_line.EnterSegment(zone, rescuing, segmentID, mapID)


end

---swamp_line.ExitSegment(zone, result, rescue, segmentID, mapID)
--Engine callback function
function swamp_line.ExitSegment(zone, result, rescue, segmentID, mapID)
    if result ~= RogueEssence.Data.GameProgress.ResultType.Cleared then
        print("Get out!")
        COMMON.EndDungeonDay(result, "entoh_town", -1, 12, 0)
    else
        SV.dreaded_depths.revisit = true
        SV.entoh_thicket.dungpoints = SV.entoh_thicket.dungpoints + 120
        COMMON.EndDungeonDay(result, "the_field", -1, 0, 0)
    end
end

---swamp_line.Rescued(zone, name, mail)
--Engine callback function
function swamp_line.Rescued(zone, name, mail)

end

return swamp_line

