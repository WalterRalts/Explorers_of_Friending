--[[
    init.lua
    Created: 07/11/2025 15:30:49
    Description: Autogenerated script file for the map dreaded_depths.
]]--
-- Commonly included lua functions and data
require 'explorers_of_friending.common'

-- Package name
local dreaded_depths = {}

-------------------------------
-- Zone Callbacks
-------------------------------
---dreaded_depths.Init(zone)
--Engine callback function
function dreaded_depths.Init(zone)


end

---dreaded_depths.EnterSegment(zone, rescuing, segmentID, mapID)
--Engine callback function
function dreaded_depths.EnterSegment(zone, rescuing, segmentID, mapID)


end

---dreaded_depths.ExitSegment(zone, result, rescue, segmentID, mapID)
--Engine callback function
function dreaded_depths.ExitSegment(zone, result, rescue, segmentID, mapID)
    if result ~= RogueEssence.Data.GameProgress.ResultType.Cleared then
        DUN_failure = true
        print("Get out!")
        COMMON.EndDungeonDay(result, "entoh_town", -1, 12, 0)
    else
        DUN_failure = false
        SV.dreaded_depths.revisit = true
        SV.entoh_thicket.dungpoints = SV.entoh_thicket.dungpoints + 120
        COMMON.EndDungeonDay(result, "swamp_town", -1, 0, 0)
    end

end

---dreaded_depths.Rescued(zone, name, mail)
--Engine callback function
function dreaded_depths.Rescued(zone, name, mail)
    
end

return dreaded_depths

